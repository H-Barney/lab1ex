<!doctype html>
<html>
   <head>
   <title>Extra Information</title>
   </head>
   <body>
       {% if all_users %}
       {% for user in all_users %}
      <h5> Details of {{ user[0] }}</h5>
      <h5> zID: {{ user[1]  }}</h5>
      <h5> Description: {{ user[2] }}</h5>
      {% endfor %}
      {%else%}
      No students to show
      {% endif %}
   </body>
</html>
